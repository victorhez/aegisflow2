import {NextResponse} from 'next/server'; import {runScenario} from '@/lib/orchestrator'; export const dynamic='force-dynamic'; export async function POST(req:Request){const body=await req.json().catch(()=>({}));return NextResponse.json(runScenario(body.scenario));}
